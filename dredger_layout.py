# -*- coding: utf-8 -*-
import csv,json 
json_file=input(' Enter the json file ')
with open("json_file) as f1:
    z = json.load(f1)    # json file is now read , z has json data and is pretty much a dictionary
    
